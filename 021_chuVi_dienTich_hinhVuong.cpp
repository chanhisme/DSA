// Nhập vào cạnh a của một hình vuông,
// tính chu vi và diện tích của hình vuông đó theo công thức cu = ax4, dt = axa.
#include <iostream>
using namespace std;
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    //mainCode
    int a; cin >> a;
    int cv = a * 4;
    int dt = a*a;
    cout<<cv<<" "<<dt;






    return 0;
}